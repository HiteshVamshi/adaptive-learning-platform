# Add only if you later integrate external APIs or databases.
# For Streamlit Community Cloud, paste these values into the app Secrets manager
# instead of committing a real secrets.toml file.

[app]
adaptive_learning_auto_build = true
